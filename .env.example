# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase — project ref: ahiweuzganjsbjanrlwu
NEXT_PUBLIC_SUPABASE_URL=https://ahiweuzganjsbjanrlwu.supabase.co

# Client-side key. Prefer the modern publishable key (sb_publishable_...).
# The legacy anon JWT still works for compatibility; set one of these.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side ONLY. Never expose to the client, never commit a real value.
# Needed later for admin/bootstrap operations (create first congregation + admin).
SUPABASE_SERVICE_ROLE_KEY=
